# Architecture (Under the Hood)

The framework is built around modern UI development patterns. It compiles to WebAssembly, combining the strict typing and performance of Go with the best architectural practices from the modern JavaScript ecosystem (such as SolidJS, Radix UI, and Tailwind CSS).

Our core philosophy is simple: **UI is a pure, strictly typed function of reactive state.**

## TBD

